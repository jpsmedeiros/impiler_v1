num = @{ int ~ ("." ~ digit*)? ~ (^"e" ~ int)? }
    int = { ("+" | "-")? ~ digit+ }
    digit = { '0'..'9' }

boolean = { "true" | "false" }

arithop = _{ add | subtract | multiply | divide }
    add      = { "+" }
    subtract = { "-" }
    multiply = { "*" }
    divide   = { "/" }


boolop = _{ equal | and | or }
    equal = { "=" }
    and   = { "/\u{005C}" }
    or    = { "\u{005C}/"}

    
iop = _{ greater_than | greater_equal | less_than | less_equal }
    greater_than  = { ">" }
    greater_equal = { ">=" }
    less_than     = { "<" }
    less_equal    = { "<=" } 

neg   = { "~" }

exp = _{ aexp | bexp }
aexp = { aterm ~ (arithop ~ aterm)* }

bexp = { bterm ~ (boolop ~ bterm)*}

aterm = _{ num | "(" ~ aexp ~ ")" | id}
bterm = _{ boolean | "(" ~ bexp ~ ")" | neg ~ bterm }

identifier_char = _{ 'a'..'z' | 'A'..'Z' | '0'..'9' | "_" }
id      =  {
    !("true" | "false" ) ~ ('a'..'z' | 'A'..'Z') ~ identifier_char* |
    "_" ~ identifier_char+
}

cmd = { cmdterm ~ ";" ~ cmd | cmdterm}

cmdterm = _{ id ~ ":=" ~ exp |  "while" ~ "(" ~ bexp ~ ")" ~ "do" ~ "{" ~ cmd ~ "}"}

impiler = _{ soi ~ (exp | cmd) ~ eoi }

whitespace = _{ " " | "\t" }